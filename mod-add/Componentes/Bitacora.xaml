<UserControl x:Class="mod_add.Componentes.Bitacora"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:mod_add.Componentes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid
        Background="#FFFFFF">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Button
            Grid.Row="0"
            Grid.Column="5"
            Margin="5"
            x:Name="Exportar"
            Click="Exportar_Click"
            Style="{DynamicResource ButtonBorderRoundStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30*"/>
                    <ColumnDefinition Width="70*"/>
                </Grid.ColumnDefinitions>
                <Image
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Source="/mod-add;component/Recursos/cuenta.png"/>
                <TextBlock
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Padding="5,0,5,0"
                    FontWeight="Bold"
                    Text="Exportar"/>
            </Grid>
        </Button>
        
        <Grid
            Grid.Row="1"
            Grid.Column="0"
            Grid.ColumnSpan="7">
            <DataGrid
                Name="RegistrosBitacora"
                Margin="5"
                ItemsSource="{Binding RegistrosBitacora}"
                Style="{DynamicResource DataGridStyle1}">
                <DataGrid.Columns>
                    <DataGridTextColumn
                        Header="Fecha de proceso"
                        Binding="{Binding FechaProceso}"/>
                    <DataGridTextColumn
                        Header="Fecha inicial (venta)"
                        Binding="{Binding FechaInicialVenta}"/>
                    <DataGridTextColumn
                        Header="Fecha final (venta)"
                        Binding="{Binding FechaFinalVenta}"/>
                    <DataGridTextColumn
                        Header="Total cuentas"
                        Binding="{Binding TotalCuentas}"/>
                    <DataGridTextColumn
                        Header="Cuentas modificadas"
                        Binding="{Binding CuentasModificadas}"/>
                    <DataGridTextColumn
                        Header="Importe anterior"
                        Binding="{Binding ImporteAnterior}"/>
                    <DataGridTextColumn
                        Header="Importe nuevo"
                        Binding="{Binding ImporteNuevo}"/>
                    <DataGridTextColumn
                        Header="Diferencia %"
                        Binding="{Binding Diferencia}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
